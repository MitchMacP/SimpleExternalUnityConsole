<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Main UI -->
    
    <ControlTemplate x:Key="ButtonTemplate" TargetType="Button">
        <Border x:Name="BtnBorder" 
                Background="#383838" 
                CornerRadius="3" 
                Padding="10,5"
                TextElement.Foreground="#EEEEEE">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#424242"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#6A6A6A"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="BtnBorder" Property="Background" Value="#202020"/>
                <Setter TargetName="BtnBorder" Property="TextElement.Foreground" Value="#4C4C4C"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ClearButtonTemplate" TargetType="Button">
        <Border x:Name="BtnBorder" 
                Background="#383838" 
                CornerRadius="3,3,0,0" 
                Padding="10,5"
                TextElement.Foreground="#EEEEEE">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#424242"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#6A6A6A"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="BtnBorder" Property="Background" Value="#202020"/>
                <Setter TargetName="BtnBorder" Property="TextElement.Foreground" Value="#4C4C4C"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ToggleButtonTemplate" TargetType="ToggleButton">
        <Border x:Name="BtnBorder" 
            Background="#3C3C3C" 
            CornerRadius="3,3,0,0" 
            Margin="1,0,0,0"
            Padding="10,5"
            TextElement.Foreground="#EEEEEE">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#464646"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#6A6A6A"/>
            </Trigger>

            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="BtnBorder" Property="Background" Value="#505050"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ListBoxTemplate" TargetType="ListBox">
        <Border x:Name="ListBoxBorder" 
                    Background="#3C3C3C"
                    TextElement.Foreground="#EEEEEE"
                    CornerRadius="0">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </ScrollViewer>
        </Border>
    </ControlTemplate>

    <Style x:Key="UnityConsoleItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Foreground" Value="#EEEEEE"/>
        <Setter Property="BorderBrush" Value="#2D2D2D"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="#3F3F3F"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4D4D4D"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#1F4E79"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="UnityInspectorTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="#2A2A2A" />
        <Setter Property="Foreground" Value="#D4D4D4"/>
        <Setter Property="BorderBrush" Value="#242424"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="UndoLimit" Value="0"/>
        <Setter Property="SelectionBrush" Value="#1F4E79"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="BaseBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="0,0,5,5">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                  Focusable="false" 
                                  HorizontalScrollBarVisibility="Disabled" 
                                  VerticalScrollBarVisibility="Auto"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BaseBorder" Property="BorderBrush" Value="#4D4D4D"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="BaseBorder" Property="BorderBrush" Value="#007ACC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Settings -->
    
    <Style TargetType="TextBlock" x:Key="SettingsHeader">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="#EEEEEE"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="15"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style TargetType="Border" x:Key="SettingsAboutText">
        <Setter Property="Background" Value="#3C3C3C"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="SettingPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

    <ControlTemplate x:Key="validationTemplate">
        <Grid>
            <Border BorderThickness="1" BorderBrush="Red">
                <AdornedElementPlaceholder x:Name="TextBoxPlaceholder" />
            </Border>

            <Canvas HorizontalAlignment="Right" VerticalAlignment="Top">
                <Label Foreground="Red" 
                   FontSize="10" 
                   Canvas.Top="-22" 
                   Canvas.Left="-90">
                    Please insert an integer
                </Label>
            </Canvas>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>